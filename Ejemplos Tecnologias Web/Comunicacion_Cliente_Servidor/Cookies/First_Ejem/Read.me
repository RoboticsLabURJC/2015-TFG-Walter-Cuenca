En este ejemplo un navegador se conecta por primera vez a un servidor este guardara en cookies (user,hora+fecha) por si vuelve 
a conectarse en el servidor aparecera la hora antigua de entrada y la actual para comprobar que el funcionamiento es correcto.
